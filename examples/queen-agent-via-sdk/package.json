{
  "name": "@colony-harness/example-queen-agent-via-sdk",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm build && node dist/index.js",
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@colony-harness/controlplane-runtime": "workspace:*",
    "@colony-harness/controlplane-sdk-adapter": "workspace:*",
    "colony-harness": "workspace:*"
  }
}
